#!/bin/bash

# NEW_GOPATH= ${HOME}/VeneerToolkit/dev/gapi-dev/discovery-artifact-manager/tools

get_gopath() {
  echo $0
  local this_dir="$(readlink -f $0)"
  echo $this_dir
  this_dir="$(dirname $this_dir)"
  echo "${this_dir}/src"
}

gopath_includes() {
  echo foo
}

get_gopath

